# CORS 配置示例
# 允许的域名列表，用逗号分隔
# 设置为 * 表示允许所有域名（不推荐用于生产环境）
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com,http://localhost:3000

# 是否允许携带凭证（cookies, authorization headers等）
# 设置为 true 时，Access-Control-Allow-Origin 不能设置为 *
CORS_ALLOW_CREDENTIALS=false

# 其他配置示例：
# 开发环境 - 允许所有域名
# CORS_ALLOWED_ORIGINS=*

# 生产环境 - 只允许特定域名
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# 多域名配置示例
# CORS_ALLOWED_ORIGINS=https://example.com,https://www.example.com,https://api.example.com 